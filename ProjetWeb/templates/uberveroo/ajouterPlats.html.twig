{%extends 'base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Ajouter plats
{% endblock %}

{% block body %}
<h1>Ajouter un plat</h1>

{{ form_start(form) }}

{{ form_row(form.nom) }}

{{ form_row(form.description) }}

{{ form_row(form.prix) }}

{{ form_row(form.photo) }}






{#

{{ form_end(form) }}
{{ form_end(formPlats) }}
{{ form_start(formPlats) }}

#}
<button type="submit" class="btn btn-success">Ajouter Plat</button>
{{ form_end(form, {'render_rest': false}) }}
<a href="{{path('restaurants') }}" class="btn btn-primary">retour</a>
{% endblock %}